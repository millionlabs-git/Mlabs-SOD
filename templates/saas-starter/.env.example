# =============================================================================
# Environment Variables
# =============================================================================
# Copy to .env for local dev. On Replit, set these in the Secrets tab.
# On Fly.io, set via: flyctl secrets set KEY=VALUE
# =============================================================================

# Database
# - Local dev: point to your local PostgreSQL
# - Replit:    auto-set when you add a PostgreSQL database via Tools > Database
# - Fly.io:   set via flyctl secrets, typically a Neon connection string
DATABASE_URL=postgresql://user:password@localhost:5432/saas_app

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-random-string

# Server
PORT=3000
NODE_ENV=development
